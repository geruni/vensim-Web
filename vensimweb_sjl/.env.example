# Ruta del archivo de base de datos SQLite.
# Si no se define, se usa db/vensimweb_sjl.db (dentro del propio proyecto).
# DB_PATH=db/vensimweb_sjl.db

# Ruta del archivo del modelo Vensim (.mdl).
# Si no se define, se usa model/residuos_sjl.mdl (dentro del propio proyecto).
# MDL_PATH=model/residuos_sjl.mdl
